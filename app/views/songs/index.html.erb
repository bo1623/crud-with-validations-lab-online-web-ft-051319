
<table>
  <tr>
    <th>Song Title</th>
    <th>Delete</th>
  </tr>
<%@songs.each do |song|%>
  <tr>
    <td><%= song.title %></td>
    <td><%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %></td>
  </tr>
<%end%>
